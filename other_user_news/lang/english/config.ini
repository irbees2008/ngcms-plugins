description = "This plugin allows you to display other news items from site users."
group.config = "<b>Plugin settings</b>"
localsource = "Select the directory from which the plugin will load display templates"
localsource#desc = "<b>Site template</b> — the plugin will attempt to load templates from the site's global template directory; if unavailable, templates will be taken from the plugin's own directory<br /><b>Plugin</b> — templates will always be loaded from the plugin's own directory"
group.source = "<b>Display settings</b>"
localsource.opt.site = "Site template"
localsource.opt.plugin = "Plugin"
